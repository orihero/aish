{
  "name": "job-board",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "cd admin && yarn install && cd ../backend && yarn install && cd ../frontend && yarn install",
    "dev": "concurrently \"cd admin && yarn dev\" \"cd backend && yarn dev\" \"cd frontend && yarn start\"",
    "seed": "cd backend && yarn seed",
    "setup": "cp admin/.env.example admin/.env && cp backend/.env.example backend/.env",
    "pm2:start": "pm2 start ecosystem.config.js",
    "pm2:start:prod": "pm2 start ecosystem.config.js --env production",
    "pm2:stop": "pm2 stop ecosystem.config.js",
    "pm2:restart": "pm2 restart ecosystem.config.js",
    "pm2:reload": "pm2 reload ecosystem.config.js",
    "pm2:delete": "pm2 delete ecosystem.config.js",
    "pm2:logs": "pm2 logs",
    "pm2:status": "pm2 status",
    "pm2:monit": "pm2 monit"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "class-variance-authority": "^0.7.1"
  }
}
